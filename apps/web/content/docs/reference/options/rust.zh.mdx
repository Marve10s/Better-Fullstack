---
title: Rust 选项
description: Rust 生态系统中 Web frameworks、frontend、ORM、API、CLI、logging、services 和 workflow flags 的选项值。
---

请在 CLI commands、MCP payloads 和 Stack Builder links 中，将这些值与 `--ecosystem rust` 一起使用。

<Callout kind="info" title="生成行为">
Rust 模板以 Cargo 为先。Framework、frontend、ORM、API、CLI、logging、error handling、libraries、services 和 workflow flags 都会根据选中的 stack 进行验证。
</Callout>

## 选项表

下表从规范选项 metadata 生成，因此会与 CLI 和 Stack Builder 保持同步。

<OptionCategoryTable ecosystem="rust" />
