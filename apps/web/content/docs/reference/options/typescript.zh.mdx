---
title: TypeScript 选项
description: TypeScript 生态系统中 frontend、backend、services、UI、tooling、addons 和 deploy 的选项值。
---

TypeScript 是 Better Fullstack 覆盖最广的生态系统。请在命令、MCP payloads 和 Stack Builder links 中使用这些 CLI 值。

<Callout kind="info" title="兼容性说明">
表格列出有效值，不代表每一种有效组合。tRPC 面向 React。`backend: self` 用于 fullstack frameworks，并与 `runtime: none` 搭配。Workers runtime、UI libraries、providers、addons 和 deploy targets 都有 stack-specific 约束。
</Callout>

React Native 和 Expo 选项位于 [React Native 选项](/docs/reference/options/react-native/)页面。

## 选项表

下表从规范选项 metadata 生成，因此会与 CLI 和 Stack Builder 保持同步。

<OptionCategoryTable ecosystem="typescript" />
