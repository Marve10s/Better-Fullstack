---
title: "React Native Параметри"
description: "Значення параметрів Expo і React Native для зовнішнього інтерфейсу, навігації, інтерфейсу користувача, зберігання, тестування, push, OTA та посилань."
---

Параметри React Native вибираються за допомогою `--ecosystem react-native`. Вони створюють програму Expo і не розкривають категорії веб-інтерфейсу, серверної частини, бази даних або веб-розгортання TypeScript.

<Callout kind="info" title="Compatibility notes">
`native-uniwind` selects `mobile-ui: uniwind`. `native-unistyles` selects `mobile-ui: unistyles`. Tamagui and gluestack-ui are intended for `native-bare` to avoid conflicting style systems.
</Callout>

## Option table

The table below is generated from the canonical option metadata so it stays in sync with the CLI and Stack Builder.

<OptionCategoryTable ecosystem="react-native" />
