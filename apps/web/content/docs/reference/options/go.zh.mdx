---
title: Go 选项
description: Go 生态系统中 frameworks、ORM、API、CLI、logging、auth、services 和 workflow flags 的选项值。
---

请在 CLI commands、MCP payloads 和 Stack Builder links 中，将这些值与 `--ecosystem go` 一起使用。

<Callout kind="info" title="生成行为">
Go 模板会把 API、CLI、logging、auth、libraries、services 和 workflow flags 与 TypeScript Web 选项分开。GoBetterAuth 面向 Go 项目，并在这里记录。
</Callout>

## 选项表

下表从规范选项 metadata 生成，因此会与 CLI 和 Stack Builder 保持同步。

<OptionCategoryTable ecosystem="go" />
