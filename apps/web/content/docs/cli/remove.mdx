---
title: Remove Command
description: Safely remove one exact non-primary stack capability through a reviewed, recoverable transaction.
translationStatus: pending
updated: 2026-08-12
---

`remove` removes one selected capability from an existing Better Fullstack project. It plans by
default, names the affected config fields and generated files, and returns a review token. No files
are written during planning.

Use the exact stack-part spec reported by `status --json` or `bts.jsonc`:

<PMTabs
  npm="npx -y create-better-fullstack@latest remove backend.auth:typescript:better-auth"
  pnpm="pnpm dlx create-better-fullstack@latest remove backend.auth:typescript:better-auth"
  bun="bunx create-better-fullstack@latest remove backend.auth:typescript:better-auth"
  yarn="yarn dlx create-better-fullstack@latest remove backend.auth:typescript:better-auth"
/>

After reviewing the plan, repeat the same target with `--apply --review-token <token>`. The token is
bound to the proposed config and file operations; missing or stale tokens fail closed. Apply uses
the same transaction and recovery contract as `add` and `update`.

Primary frontend, backend, mobile, and database roles cannot be removed. Replace those choices with
[`add`](/docs/cli/add/) so compatibility and migration risks are reviewed. When a removal itself is
architecture-sensitive, apply also requires `--acknowledge-architecture-change`; Better Fullstack
never migrates application data or schemas automatically.
