---
title: React Native
description: Мобільні риштування Expo і React Native з виробничою інтеграцією.
---

React Native — це спеціальна мобільна екосистема. Він об’єднує проекти Expo та зберігає вибір мобільних пристроїв окремо від категорій веб-інтерфейсу, серверної частини, бази даних і розгортання TypeScript.

## передумови

- Node.js 20+.
- npm, pnpm, Yarn або Bun.
- Expo-сумісні інструменти iOS/Android, якщо вам потрібен симулятор або перевірка пристрою.

## Приклад сценарію

```npm
npm create better-fullstack@latest mobile-app -- \
  --ecosystem react-native \
  --frontend native-bare \
  --mobile-navigation expo-router \
  --mobile-ui tamagui \
  --mobile-storage mmkv \
  --mobile-testing maestro-react-native-testing-library \
  --mobile-push expo-notifications \
  --mobile-ota expo-updates \
  --mobile-deep-linking expo-linking \
  --ai-docs none \
  --no-install
```

## Категорії мобільних риштувань

| Категорія | Цінності |
| --- | --- |
| Рідний інтерфейс | `native-bare` `native-uniwind` `native-unistyles` |
| Навігація | `expo-router` `react-navigation` `none` |
| інтерфейс користувача | `tamagui` `gluestack-ui` `uniwind` `unistyles` `none` |
| Зберігання | `mmkv` `none` |
| Тестування | `maestro` `react-native-testing-library` `maestro-react-native-testing-library` `none` |
| штовхати | `expo-notifications` `none` |
| OTA | `expo-updates` `none` |
| Глибинне посилання | `expo-linking` `none` |

## Згенеровані інтеграції

- Конфігурація Expo з налаштуваннями схеми/часу виконання, коли вибрано глибоке посилання або OTA.
- Expo Файли маршруту маршрутизатора або точка входу React Навігація, залежно від навігації.
- Файли постачальника мобільного інтерфейсу/теми для Tamagui, Gluestack UI, Uniwind або Unistyles.
- Помічники клієнта MMKV, коли вибрано сховище.
- Приклади бібліотеки тестування Jest Expo та React Native, потоки Maestro або обидва.
- Помічники реєстрації push-повідомлень для Expo Notifications.
- Помічники оновлення OTA для Expo Updates.
- `.env.example` з угодами `EXPO_PUBLIC_` для конфігурації мобільного клієнта.

## Примітки щодо сумісності

- React Native не надає TypeScript категорій веб-інтерфейсу, бекенда, бази даних, веб-інтерфейсу користувача, веб-розгортання або розгортання сервера.
- Режими інтерфейсу користувача Uniwind і Unistyles прив’язані до відповідних варіантів інтерфейсу Expo.
- Інтерфейс користувача Tamagui та Gluestack використовує простий варіант Expo, щоб уникнути конфліктних налаштувань стилів.
- Глибинне посилання вмикається автоматично, коли для потоку автентифікації мобільного пристрою потрібні приклади URI перенаправлення.
