---
title: "Gin з PostgreSQL і GORM"
description: "Створіть проект Go API з Gin, PostgreSQL, GORM, журналювання Zap і чистою структурою проекту Better Fullstack."
updated: 2026-05-12
category: Go
tags:
  - gin
  - postgres
  - gorm
  - go
keywords:
  - gin postgres starter
  - go api boilerplate
  - gin gorm starter
  - create go api project
  - go postgres api
---

Використовуйте цей стек, якщо вам потрібен Go HTTP API зі знайомим маршрутизатором, доступом до реляційної бази даних і прагматичною структурою проекту.

```npm
npm create better-fullstack@latest my-go-api -- \
  --ecosystem go \
  --go-web-framework gin \
  --database postgres \
  --go-orm gorm \
  --go-logging zap
```

<GuideStackSnapshot
  title="Generated stack snapshot"
  description="The generated Go service is organized around these responsibilities."
  items={[
    { label: "HTTP", value: "Gin", detail: "Routing, request binding, middleware, and responses." },
    { label: "Data", value: "PostgreSQL + GORM", detail: "Model mapping and persistence for CRUD-heavy services." },
    { label: "Logging", value: "Zap", detail: "Structured logs that can carry request and dependency context." },
    { label: "Shape", value: "cmd + internal", detail: "A backend service layout that keeps app code behind package boundaries." },
  ]}
/>

## Що це створює

- Проект Go API з використанням Gin.
- PostgreSQL як параметр бази даних.
- GORM для доступу до бази даних.
- Зап для структурованої рубки.
- Згенерований макет проекту, готовий до розробки на сервері.

## Згенерована форма

Цей стек створює звичайний Go HTTP API. Gin керує маршрутизацією та проміжним програмним забезпеченням, GORM володіє відображенням моделі та постійністю, PostgreSQL є резервним сховищем, а Zap надає структуровані журнали.

Найчистіша межа створеної програми:

- Обробники HTTP аналізують введення та повертають відповіді.
- Обслуговує власні бізнес-рішення.
- Моделі GORM описують збережені записи.
- Налаштування бази даних і реєстратора відбувається один раз під час запуску.

## Репрезентативне дерево файлів

```txt
my-go-api/
  go.mod
  .env.example
  cmd/
    server/
      main.go
  internal/
    config/
      config.go
    database/
      postgres.go
    handlers/
      health.go
      users.go
    models/
      user.go
    services/
      users.go
```

Згенеровані проекти Go відрізняються залежно від набору параметрів, але це форма, яку потрібно зберегти в міру розвитку програми: зберігайте код рамки поруч із обробниками, а код постійності — за службами чи сховищами.

## Приклади обробника та моделі

Обробник Gin повинен прив’язувати дані запиту, викликати код програми та повертати чіткий статус HTTP:

```go
package handlers

import (
	"net/http"

	"github.com/gin-gonic/gin"
)

type CreateUserRequest struct {
	Email string `json:"email" binding:"required,email"`
	Name  string `json:"name" binding:"required"`
}

func (h *UserHandler) Create(c *gin.Context) {
	var input CreateUserRequest
	if err := c.ShouldBindJSON(&input); err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})
		return
	}

	user, err := h.Users.Create(c.Request.Context(), input.Email, input.Name)
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": "create user"})
		return
	}

	c.JSON(http.StatusCreated, user)
}
```

Відповідна модель GORM може залишатися маленькою та явною:

```go
package models

import "time"

type User struct {
	ID        uint      `json:"id" gorm:"primaryKey"`
	Email     string    `json:"email" gorm:"uniqueIndex;not null"`
	Name      string    `json:"name" gorm:"not null"`
	CreatedAt time.Time `json:"createdAt"`
	UpdatedAt time.Time `json:"updatedAt"`
}
```

Реєстрація маршруту повинна утримувати пов’язані кінцеві точки разом:

```go
func RegisterRoutes(router *gin.Engine, users *UserHandler) {
	router.GET("/health", Health)

	api := router.Group("/api")
	api.POST("/users", users.Create)
	api.GET("/users/:id", users.Get)
}
```

## Примітки щодо сумісності

<GuideCompatibilityNote kind="supported" title="Compatibility rule">
  Тримайте `--go-web-framework gin`, `--database postgres` і `--go-orm gorm` разом для цього посібника. Якщо ви зміните `--go-orm sqlc`, очікуйте файлів SQL і згенерованих типів запитів замість моделей GORM. Якщо ви встановите `--go-web-framework none`, це стане несерверним проектом, і приклади обробки Gin більше не застосовуються.
</GuideCompatibilityNote>

## Коли його вибрати

Виберіть Gin, якщо вам потрібен прямий Go API із загальними шаблонами проміжного програмного забезпечення та низькими витратами на структуру.

## Компроміси

Gin і GORM є знайомими для багатьох команд Go. Якщо ви віддаєте перевагу меншій поверхні маршрутизатора або іншому стилю API, порівняйте інші параметри фреймворка Go у конструкторі.

GORM є продуктивним для CRUD-додатків, але він може приховати деталі SQL. Для служб із інтенсивними запитами, де точний SQL є частиною контракту, порівняйте посібник Echo з SQLC.

## Примітки щодо тестування та розгортання

Використовуйте `httptest` для тестів обробників і справжню базу даних PostgreSQL для інтеграційних тестів навколо поведінки GORM.

```bash
go test ./...
```

Перед розгортанням переконайтеся, що згенерована програма зчитує URL-адресу бази даних із середовища та що поведінка міграції або автоматичної міграції відповідає політиці випуску вашої команди. Використовуйте поля Zap для ідентифікаторів запитів, ідентифікаторів користувачів і помилок залежностей, щоб журнали виробництва можна було фільтрувати без аналізу рядків.

## Усунення несправностей

- Якщо запити повертають 400, перевірте теги прив’язки Gin перед налагодженням коду служби.
- Якщо вставки не вдаються з повторюваними помилками ключа, перевірте індекси GORM і обмеження PostgreSQL разом.
- Якщо програма не може підключитися до PostgreSQL, перевірте хост, порт, режим SSL і згенеровані назви змінних середовища.
- Якщо журнали неструктуровані, переконайтеся, що реєстратор Zap передається в обробники та служби замість використання `fmt.Println`.

## Наступні кроки

- Відкрийте [Stack Builder](/new?eco=go&gwf=gin&db=postgres&gorm=gorm&glog=zap).
- Прочитайте [Go документацію про екосистему](/docs/ecosystems/go/).
- Перегляньте [CLI посилання на створення](/docs/cli/create/).
