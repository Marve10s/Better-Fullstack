# {{projectName}}

ASP.NET Core project generated by Better Fullstack.

## Commands

```bash
dotnet restore
dotnet run
{{#if dotnetHasTests}}dotnet test{{/if}}
```

## Stack

- Framework: {{dotnetWebFramework}}
- Data access: {{dotnetOrm}}
- Auth: {{dotnetAuth}}
- API style: {{dotnetApi}}
- Realtime: {{dotnetRealtime}}
- Observability: {{#if dotnetObservability.length}}{{dotnetObservability}}{{else}}none{{/if}}
- Libraries: {{#if dotnetLibraries.length}}{{dotnetLibraries}}{{else}}none{{/if}}
- Deploy: {{dotnetDeploy}}
{{#if dotnetLibraries.length}}

The selected application libraries are restored as pinned NuGet references. They are intentionally
left unregistered until you supply the app-specific transports, credentials, endpoints, and domain
types their production configuration requires.
{{/if}}
