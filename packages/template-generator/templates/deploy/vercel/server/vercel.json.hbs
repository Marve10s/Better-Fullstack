{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && {{#if (eq packageManager "bun")}}bun run build --filter=server{{else}}npx turbo build --filter=server{{/if}}",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
