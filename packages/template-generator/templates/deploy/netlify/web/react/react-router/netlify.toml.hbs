# Netlify configuration for {{projectName}} React Router app
# https://docs.netlify.com/build/frameworks/framework-setup-guides/react-router/
#
# In the Netlify dashboard, set the site's base directory to "apps/web".

[build]
  command = "{{packageManager}} run build"
  publish = "build/client"
