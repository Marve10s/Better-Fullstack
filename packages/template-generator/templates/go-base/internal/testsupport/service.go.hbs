{{#if (includes goTesting "mockery")}}
package testsupport

import "context"

//go:generate go run github.com/vektra/mockery/v3 --name Service --output mocks
type Service interface {
	Execute(context.Context, string) error
}
{{/if}}
