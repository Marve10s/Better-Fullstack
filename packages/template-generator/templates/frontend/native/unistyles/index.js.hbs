import './unistyles';
{{#if (or (eq mobileNavigation "react-navigation") (eq mobileNavigation "none"))}}
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
{{else}}
import 'expo-router/entry';
{{/if}}
