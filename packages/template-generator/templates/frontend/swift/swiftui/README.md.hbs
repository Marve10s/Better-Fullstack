# {{projectName}} iOS app

Native SwiftUI application for iPhone and iPad. Generate the checked-in Xcode project from the deterministic XcodeGen spec, then build it:

```bash
xcodegen generate
xcodebuild -scheme BetterFullstackApp -sdk iphonesimulator build
```
