# {{projectName}} mobile

Native Android application built with Kotlin and Jetpack Compose.

```bash
./gradlew assembleDebug
```

Open this directory in Android Studio to run the app on an emulator or device.
