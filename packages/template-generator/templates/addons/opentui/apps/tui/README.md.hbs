# OpenTUI

This terminal UI app uses OpenTUI's core renderer.

## Scripts

- `{{packageManager}} run dev` starts the TUI with Bun.
- `{{packageManager}} run check-types` type-checks the TUI source.

Press `Ctrl+C` to exit the running TUI.
