{{#if isJavaSpringBoot}}
PORT=8080
SPRING_PROFILES_ACTIVE=dev

# Allowed web origin for CORS (unset = allow any origin)
# CORS_ORIGIN=http://localhost:3001
{{#if hasJavaSecurity}}
APP_BASIC_USERNAME=admin
APP_BASIC_PASSWORD=change-me
{{/if}}
{{/if}}
