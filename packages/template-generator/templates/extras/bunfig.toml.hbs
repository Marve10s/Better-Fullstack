[install]
{{#if (or (includes frontend "nuxt") (includes frontend "redwood"))}}
linker = "hoisted" # Nuxt and Redwood rely on dependencies hoisted by their official installers
{{else}}
linker = "isolated"
{{/if}}
